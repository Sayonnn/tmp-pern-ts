# upstream backend_service {
#     server backend:5000;     # ✅ internal container port
# }

# server {
#     listen 80;
#     server_name localhost;   # ❌ change to domain in production

#     location / {
#         try_files $uri /index.html;
#     }

#     location /api/ {
#         proxy_pass http://backend_service;   # ❌ change only if backend needs to be public; otherwise not needed
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }


# --- Production blocks commented out for now ---
# server {
#     listen 80;
#     server_name api.yourdomain.com;
#     return 301 https://$host$request_uri;
# }

# server {
#     listen 443 ssl;
#     server_name api.yourdomain.com;
#     ssl_certificate /etc/ssl/certs/fullchain.pem;
#     ssl_certificate_key /etc/ssl/private/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     location /api/ {
#         proxy_pass http://backend_service;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
